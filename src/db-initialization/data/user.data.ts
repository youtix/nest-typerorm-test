import { User } from '../../domain/user.entity';

const user1: User = {
  login: 'system',
  createdBy: 'system',
  lastModifiedBy: 'system',
};

const user2: User = {
  login: 'anonymoususer',
  createdBy: 'system',
  lastModifiedBy: 'system',
};

const user3: User = {
  login: 'admin',
  createdBy: 'system',
  lastModifiedBy: 'system',
};

const user4: User = {
  login: 'user',
  createdBy: 'system',
  lastModifiedBy: 'system',
};

export { user1 };
export { user2 };
export { user3 };
export { user4 };
